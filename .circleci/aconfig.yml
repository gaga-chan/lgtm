version: 2
jobs:
  setup_dependencies:
    docker:
      - image: circleci/python:3.9.1
    steps:
      - checkout:
      - restore_cache:
          key: deps-{{ checksum "requirements.lock" }}
      - run:
          command: |
            pip install --user -r requirements.lock
      - save_cache:
          key: deps-{{ checksum "requirements.lock" }}
          paths:
            - "~/.local"

  build:
    docker:
      - image: circleci/python:3.9.1
    steps:
      - checkout
      - restore_cache:
          key: deps-{{ checksum "requirements.lock" }}
      - run:
          command: |
            python3 -m unittest -v

workflows:
  version: 2
  all:
    jobs:
      - setup_dependencies
      - build:
          requires:
            - setup_dependencies
<<<<<<< HEAD:.circleci/aconfig.yml
=======

            
            
>>>>>>> 352707e51b5455e2acc796806e7d2e5ce8496823:.circleci/config.yml
